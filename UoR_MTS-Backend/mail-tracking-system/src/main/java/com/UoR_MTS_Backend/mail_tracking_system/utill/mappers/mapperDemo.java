package com.UoR_MTS_Backend.mail_tracking_system.utill.mappers;

import org.mapstruct.Mapper;

@Mapper(componentModel = "spring")
public interface mapperDemo {
    //List<Destination> name(List<Source> source);
}
